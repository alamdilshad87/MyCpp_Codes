
#include <iostream>

using namespace std;

int main()
{
    int num;
    cout<<"Enter a Number : ";
    cin>>num;
    switch(num)
    case 1:
        if(num%6&9==0)
        {
            cout<<"the number is divisible by both."
        }

    return 0;
}
